#include <atma/lockfree_list.hpp>
#include <atma/memory.hpp>

// this file is required because without it, Visual Studio doesn't understand that
// it must traverse the header-files, and using "go to file" on an #include doesn't
// actually work, which is ridiculous, but here we are.

