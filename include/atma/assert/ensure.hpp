#pragma once
//=====================================================================
#ifdef ATMA_ENABLE_ASSERTS
//=====================================================================

	//=====================================================================
	// ATMA_ENSURE
	// -------------
	//   halts if the boolean expression @x evaluates to false. always.
	//   even in release builds.
	//=====================================================================
	#define ATMA_ENSURE(x) \
		do { \
			if ( !(x) ) \
				{ ATMA_DEBUGBREAK(); } \
		} while (0)


	//=====================================================================
	// ATMA_ENSURE_IS
	// ----------------
	//   ensures that the expression @x evaluates to @r
	//=====================================================================
	#define ATMA_ENSURE_IS(r, x) \
		do { \
			auto _xr = (x); \
			ATMA_ENSURE((r) == _xr); \
		} while (0)
		

//=====================================================================
#endif
//=====================================================================
